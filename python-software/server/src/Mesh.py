#for later

class Mesh:

    def __init__(self, num_branches = 1, num_nodes = 1, has_center = False):
        self.num_branches = num_branches
        self.has_center = has_center
        self.nodes_list 
    
    def dostuff(self, param="default"):
        pass 